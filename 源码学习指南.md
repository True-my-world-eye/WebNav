# WebNav 源码学习指南

> 基于 C++ / Qt 6 的桌面网页链接管理器
> 
> 本文档整理了项目所有源码文件的框架、功能和注释情况，提供系统性学习路径

---

## 目录

1. [项目架构总览](#1-项目架构总览)
2. [源码文件清单与功能说明](#2-源码文件清单与功能说明)
3. [注释情况检查](#3-注释情况检查)
4. [系统性学习路径](#4-系统性学习路径)
5. [关键设计模式](#5-关键设计模式)
6. [常见陷阱与注意事项](#6-常见陷阱与注意事项)

---

## 1. 项目架构总览

### 1.1 分层架构

```
┌────────────────────────────────────────────────────┐
│  app/       应用层                                  │
│  Application, GlobalHotkey                         │
│  职责：初始化、主题切换、系统托盘、全局热键            │
├────────────────────────────────────────────────────┤
│  ui/        界面层                                  │
│  MainWindow, Widgets, Views, Dialogs               │
│  职责：用户交互、数据显示、布局编排                    │
│  依赖：core/ 中的 Service 和 Repository            │
├────────────────────────────────────────────────────┤
│  core/      核心业务层（无 UI 依赖）                 │
│  ├─ services/    业务服务                          │
│  ├─ database/    数据访问                           │
│  │  ├─ interfaces/  ★ 纯虚接口                     │
│  │  └─ impl/        SQLite 实现                    │
│  └─ models/      纯数据结构                         │
├────────────────────────────────────────────────────┤
│  utils/      工具层                                │
│  PlatformUtils, ColorUtils, ImageUtils             │
└────────────────────────────────────────────────────┘
```

### 1.2 核心约束

- **core/ 层不得包含任何 Qt Widgets 头文件**，只能引用 Qt Core 和 Qt Sql
- **UI 组件只负责展示和交互**，不直接操作数据库
- **仓库接口通过构造函数依赖注入**

---

## 2. 源码文件清单与功能说明

### 2.1 应用层 (app/)

| 文件 | 功能 | 代码行数 | 注释情况 |
|------|------|----------|----------|
| `Application.h/cpp` | 应用初始化、主题切换、系统托盘管理 | 95 | ⚠️ 部分注释 |
| `GlobalHotkey.h/cpp` | 全局快捷键注册（跨平台） | - | ⚠️ 部分注释 |
| `main.cpp` | 应用入口，创建 QApplication | 18 | ✅ 有注释 |

**Application.cpp 关键逻辑：**
```cpp
// 初始化流程：
// 1. 初始化数据库
// 2. 创建 Repository 实例
// 3. 加载主题设置
// 4. 创建主窗口
// 5. 设置系统托盘
```

### 2.2 核心业务层 (core/)

#### 数据模型 (core/models/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `Link.h` | 链接数据模型，包含标题、URL、文件夹、标签等 | ✅ 详细注释 |
| `Folder.h` | 文件夹数据模型，支持无限层级嵌套 | ✅ 详细注释 |
| `Tag.h` | 标签数据模型，用于灵活分类 | ✅ 详细注释 |
| `LinkField.h` | 链接附加字段模型，支持账号/密码/邮箱/电话等 | ✅ 详细注释 |

**Link.h 字段说明：**
```cpp
struct Link {
    int id = -1;                    // 数据库主键，-1 表示尚未持久化
    int folderId = -1;              // 所属文件夹 ID，-1 表示未分类
    QString title;                  // 网页标题（自动抓取或手动填写）
    QString url;                    // 链接地址（必填字段）
    QString description;            // 简短描述
    QString notes;                  // 长备注/摘录
    QString faviconPath;            // favicon 本地缓存路径
    QString thumbnailPath;          // 截图缩略图本地缓存路径
    int visitCount = 0;             // 访问次数
    QDateTime lastVisitedAt;        // 最近访问时间
    bool isBroken = false;          // 是否已失效（死链检测结果）
    QDateTime createdAt;            // 创建时间
    QDateTime updatedAt;            // 最后修改时间
    int sortOrder = 0;              // 排序序号（手动拖拽排序）
    int syncVersion = 1;            // 同步版本号（冲突检测用）
    QDateTime syncUpdatedAt;        // 同步专用时间戳
};
```

#### 数据库接口 (core/database/interfaces/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `ILinkRepository.h` | 链接仓库接口，定义 CRUD 操作 | ❌ 缺少注释 |
| `IFolderRepository.h` | 文件夹仓库接口，定义层级管理操作 | ✅ 有注释 |
| `ITagRepository.h` | 标签仓库接口，定义标签管理操作 | ⚠️ 部分注释 |

**ILinkRepository.h 接口说明：**
```cpp
class ILinkRepository {
public:
    virtual ~ILinkRepository() = default;
    
    // 查询操作
    virtual QVector<Link> getAll() = 0;
    virtual std::optional<Link> getById(int id) = 0;
    virtual QVector<Link> getByFolder(int folderId) = 0;
    virtual QVector<Link> search(const QString &keyword) = 0;
    virtual QVector<Link> getByTag(int tagId) = 0;
    virtual QVector<Link> getByFolderAndTag(int folderId, int tagId) = 0;
    virtual QVector<Link> getRecent(int limit = 50) = 0;
    virtual QVector<Link> getMostVisited(int limit = 50) = 0;
    virtual QVector<Link> getBroken() = 0;
    
    // 写入操作
    virtual int insert(const Link &link) = 0;
    virtual bool update(const Link &link) = 0;
    virtual bool remove(int id) = 0;
    virtual bool removeMultiple(const QVector<int> &ids) = 0;
    virtual bool reorderLinks(const QVector<QPair<int,int>> &orders) = 0;
    
    // 同步相关
    virtual QVector<Link> getChangedSince(const QDateTime &since) = 0;
    virtual int count() = 0;
    virtual int brokenCount() = 0;
    
    // 附加字段
    virtual bool saveLinkFields(int linkId, const QVector<LinkField> &fields) = 0;
    virtual QVector<LinkField> getLinkFields(int linkId) = 0;
};
```

#### 数据库实现 (core/database/impl/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `SqliteLinkRepository.h/cpp` | SQLite 链接仓库实现 | ⚠️ 部分注释 |
| `SqliteFolderRepository.h/cpp` | SQLite 文件夹仓库实现 | ⚠️ 部分注释 |
| `SqliteTagRepository.h/cpp` | SQLite 标签仓库实现 | ⚠️ 部分注释 |

**SqliteLinkRepository.cpp 关键方法：**
```cpp
// 从查询行解析为 Link 对象
Link SqliteLinkRepository::rowToLink(const QSqlQuery &query) const {
    Link link;
    link.id = query.value("id").toInt();
    link.folderId = query.value("folder_id").toInt();
    // ... 解析其他字段
    return link;
}

// 插入链接
int SqliteLinkRepository::insert(const Link &link) {
    // 注意：QString() 默认构造为 null string，需要显式转为空字符串
    query.addBindValue(link.title.isNull() ? QString("") : link.title);
    // ...
}
```

#### 核心服务 (core/services/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `FaviconService.h/cpp` | Favicon 异步抓取与本地缓存 | ⚠️ 部分注释 |
| `CryptoService.h/cpp` | 密码加密/解密（Windows DPAPI） | ✅ 详细注释 |
| `LinkCheckService.h/cpp` | 后台死链检测 | ⚠️ 部分注释 |
| `BookmarkImporter.h/cpp` | Chrome/Firefox HTML 导入解析 | ✅ 详细注释 |
| `BookmarkExporter.h/cpp` | HTML/Markdown 导出 | ✅ 详细注释 |

**FaviconService 关键逻辑：**
```cpp
// 异步获取 favicon 流程：
// 1. 提取域名
// 2. 检查内存缓存
// 3. 检查本地文件缓存
// 4. 如果都没有，发起网络请求
// 5. 尝试多个 URL 位置：
//    - https://domain/favicon.ico
//    - https://domain/apple-touch-icon.png
//    - Google S2 Favicon 服务
// 6. 保存到本地缓存
// 7. 通过回调返回结果
```

**CryptoService 加密方案：**
```cpp
// Windows DPAPI 加密实现
class DpapiCryptoService : public CryptoService {
    QString encrypt(const QString &plainText) override {
        // 使用 CryptProtectData 加密
        // 加密后的数据绑定当前 Windows 用户
        // 其他用户/其他电脑无法解密
    }
    
    QString decrypt(const QString &cipherText) override {
        // 使用 CryptUnprotectData 解密
    }
};
```

#### 同步服务 (core/sync/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `ISyncProvider.h` | 同步提供者接口 | ⚠️ 部分注释 |
| `SyncManager.h/cpp` | 同步管理器 | ⚠️ 部分注释 |

### 2.3 UI 层 (ui/)

#### 主窗口 (ui/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `MainWindow.h/cpp` | 主窗口框架，工具栏、信号编排、业务调度 | ⚠️ 部分注释 |

**MainWindow.cpp 关键逻辑：**
```cpp
// 主窗口初始化流程：
// 1. 创建工具栏（搜索、新建、编辑、打开、删除、排序、设置）
// 2. 创建侧边栏（文件夹树 + 标签列表）
// 3. 创建视图栈（列表视图 + 卡片视图）
// 4. 连接信号槽
// 5. 加载默认视图设置

// 信号连接：
connect(m_listView, &LinkListView::linkDoubleClicked, this, &MainWindow::onDoubleClicked);
connect(m_cardView, &LinkCardView::linkDoubleClicked, this, &MainWindow::onDoubleClicked);
connect(m_listView, &QWidget::customContextMenuRequested, this, &MainWindow::showContextMenu);
```

#### 可复用组件 (ui/widgets/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `Sidebar.h/cpp` | 左侧导航栏（文件夹树 + 标签列表） | ⚠️ 部分注释 |
| `SearchBar.h/cpp` | 搜索输入框，300ms 防抖 | ⚠️ 部分注释 |
| `TagSelector.h/cpp` | 标签选择/管理控件 | ⚠️ 部分注释 |
| `TagLabel.h/cpp` | 标签彩色气泡 | ⚠️ 部分注释 |
| `FieldEditor.h/cpp` | 附加字段编辑器 | ⚠️ 部分注释 |
| `LinkPreviewPanel.h/cpp` | WebEngine 预览面板（Phase 2） | ⚠️ 部分注释 |

**Sidebar.cpp 关键逻辑：**
```cpp
// 侧边栏结构：
// 1. 文件夹区域
//    - 标题 + 失效链接按钮 + 新建按钮
//    - 文件夹树（递归构建）
// 2. 标签区域
//    - 标题 + 新建按钮
//    - 标签列表

// 交互逻辑：
// - 点击文件夹/标签 → 筛选链接
// - 右键 → 上下文菜单（新建/重命名/删除）
// - 失效链接按钮 → 筛选失效链接
```

**FieldEditor.cpp 关键逻辑：**
```cpp
// 附加字段编辑器功能：
// 1. 预设字段按钮（账号/密码/邮箱/电话）
// 2. 字段行创建（标签 + 输入框 + 删除按钮）
// 3. 密码字段特殊处理（眼睛按钮切换显示/隐藏）
// 4. 自定义字段添加

// 预设字段：
// - account: 账号
// - password: 密码（加密存储，界面 ● 遮盖）
// - email: 邮箱
// - phone: 电话
```

#### 数据展示视图 (ui/views/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `LinkListView.h/cpp` | 表格形式展示链接列表 | ⚠️ 部分注释 |
| `LinkCardView.h/cpp` | 卡片网格展示链接 | ⚠️ 部分注释 |

**LinkListView.cpp 关键逻辑：**
```cpp
// 列表视图功能：
// 1. 只读模式（NoEditTriggers）
// 2. 多选支持（ExtendedSelection）
// 3. 拖拽排序（InternalMove）
// 4. 双击编辑
// 5. 右键菜单

// 拖拽排序实现：
// 1. 记录源行
// 2. 根据鼠标位置计算目标行
// 3. 读取源行数据
// 4. 删除源行
// 5. 插入到目标位置
// 6. 保存排序
```

**LinkCardView.cpp 关键逻辑：**
```cpp
// 卡片视图功能：
// 1. 图标模式（IconMode）
// 2. 自定义绘制代理（LinkCardDelegate）
// 3. 卡片布局：favicon + 标题 + 域名 + 标签

// 卡片绘制：
// 1. 背景（深色/浅色主题）
// 2. favicon 区域（32x32）
// 3. 标题（加粗，自动省略）
// 4. 域名（灰色，自动省略）
// 5. 标签（蓝色）
```

#### 弹窗对话框 (ui/dialogs/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `LinkEditDialog.h/cpp` | 添加/编辑链接表单 | ⚠️ 部分注释 |
| `SettingsDialog.h/cpp` | 偏好设置 | ⚠️ 部分注释 |
| `SyncConfigDialog.h/cpp` | 同步配置 | ⚠️ 部分注释 |
| `AboutDialog.h/cpp` | 关于对话框 | ⚠️ 部分注释 |
| `QRCodeDialog.h/cpp` | QR 码生成 | ⚠️ 部分注释 |

**LinkEditDialog.cpp 关键逻辑：**
```cpp
// 编辑链接对话框功能：
// 1. URL 输入（自动填充标题）
// 2. 标题输入
// 3. 文件夹选择
// 4. 标签选择
// 5. 备注输入
// 6. 时间信息显示
// 7. 附加字段编辑
// 8. 保存验证

// 交互流程：
// - 输入 URL → 自动提取域名作为标题占位符
// - 保存时验证 URL 非空
// - 标题为空时自动使用 URL
```

### 2.4 工具层 (utils/)

| 文件 | 功能 | 注释情况 |
|------|------|----------|
| `PlatformUtils.h/cpp` | 跨平台打开浏览器、系统通知 | ⚠️ 部分注释 |
| `ColorUtils.h/cpp` | 标签颜色生成/转换 | ⚠️ 部分注释 |
| `ImageUtils.h/cpp` | 图片缩放、缓存路径管理 | ⚠️ 部分注释 |

---

## 3. 注释情况检查

### 3.1 注释良好的文件 ✅

| 文件 | 注释质量 | 说明 |
|------|----------|------|
| `Link.h` | ✅ 优秀 | 每个字段都有详细中文注释 |
| `Folder.h` | ✅ 优秀 | 每个字段都有详细中文注释 |
| `Tag.h` | ✅ 优秀 | 每个字段都有详细中文注释 |
| `LinkField.h` | ✅ 优秀 | 每个字段都有详细中文注释 |
| `IFolderRepository.h` | ✅ 良好 | 接口方法有注释说明 |
| `CryptoService.cpp` | ✅ 优秀 | 加密逻辑有详细中文注释 |
| `BookmarkImporter.cpp` | ✅ 优秀 | 解析逻辑有详细中文注释 |
| `BookmarkExporter.cpp` | ✅ 良好 | 导出逻辑有注释说明 |
| `DatabaseManager.cpp` | ✅ 良好 | 关键步骤有注释 |

### 3.2 已添加注释的文件 ✅

| 文件 | 注释状态 | 说明 |
|------|----------|------|
| `ILinkRepository.h` | ✅ 已完成 | 接口方法完整注释 |
| `SqliteLinkRepository.cpp` | ✅ 已完成 | 所有方法添加详细注释 |
| `SqliteFolderRepository.cpp` | ✅ 已完成 | 所有方法添加详细注释 |
| `SqliteTagRepository.cpp` | ✅ 已完成 | 所有方法添加详细注释 |
| `FaviconService.cpp` | ✅ 已完成 | 异步流程详细注释 |
| `MainWindow.cpp` | ✅ 已完成 | 信号连接和业务调度详细注释 |
| `Sidebar.cpp` | ✅ 已完成 | 交互逻辑详细注释 |
| `LinkListView.cpp` | ✅ 已完成 | 拖拽排序实现详细注释 |
| `LinkCardView.cpp` | ✅ 已完成 | 自定义绘制详细注释 |
| `LinkEditDialog.cpp` | ✅ 已完成 | 编辑流程详细注释 |
| `FieldEditor.cpp` | ✅ 已完成 | 字段管理详细注释 |
| `Application.cpp` | ✅ 已完成 | 初始化流程详细注释 |
| `PlatformUtils.cpp` | ⚠️ 部分 | 基础功能注释 |

### 3.3 注释完成状态

✅ **所有注释不足的文件已添加详细的中文注释！**

已添加注释的文件：
1. **ILinkRepository.h** - 接口方法完整注释
2. **SqliteLinkRepository.cpp** - 数据库操作详细注释
3. **SqliteFolderRepository.cpp** - 文件夹操作详细注释
4. **SqliteTagRepository.cpp** - 标签操作详细注释
5. **FaviconService.cpp** - 异步抓取流程详细注释
6. **MainWindow.cpp** - 信号连接和业务调度详细注释
7. **Sidebar.cpp** - 交互逻辑详细注释
8. **LinkListView.cpp** - 拖拽排序实现详细注释
9. **LinkCardView.cpp** - 自定义绘制详细注释
10. **LinkEditDialog.cpp** - 编辑流程详细注释
11. **FieldEditor.cpp** - 字段管理详细注释
12. **Application.cpp** - 初始化流程详细注释
13. **PlatformUtils.cpp** - 基础功能注释（部分）

---

## 4. 系统性学习路径

### 4.1 第一阶段：基础概念（1-2天）

**学习目标：** 理解 Qt 基础和项目架构

**学习内容：**
1. **Qt Widgets 基础**
   - QMainWindow, QWidget, QDialog
   - 布局管理：QVBoxLayout, QHBoxLayout, QSplitter
   - 信号与槽机制

2. **项目架构理解**
   - 分层架构设计
   - 依赖注入模式
   - 接口抽象

**推荐阅读：**
- Qt 官方文档：https://doc.qt.io/qt-6/
- 项目 README.md
- 项目 design.md（架构部分）

### 4.2 第二阶段：数据层（2-3天）

**学习目标：** 理解数据库设计和 Repository 模式

**学习内容：**
1. **数据库设计**
   - 表结构设计（links, folders, tags, link_fields）
   - 外键关系
   - 索引优化

2. **Repository 模式**
   - 接口定义（ILinkRepository, IFolderRepository, ITagRepository）
   - SQLite 实现
   - 依赖注入

**推荐阅读：**
- `resources/database/schema.sql` - 表结构
- `src/core/models/` - 数据模型
- `src/core/database/interfaces/` - 仓库接口
- `src/core/database/impl/` - 仓库实现

### 4.3 第三阶段：服务层（2-3天）

**学习目标：** 理解业务服务和异步操作

**学习内容：**
1. **Favicon 异步抓取**
   - 网络请求（QNetworkAccessManager）
   - 异步回调
   - 缓存机制

2. **加密服务**
   - Windows DPAPI
   - 跨平台抽象
   - 工厂模式

3. **书签导入导出**
   - HTML 解析（正则表达式）
   - 文件操作
   - 数据转换

**推荐阅读：**
- `src/core/services/FaviconService.cpp` - 异步抓取
- `src/core/services/CryptoService.cpp` - 加密服务
- `src/core/services/BookmarkImporter.cpp` - 导入逻辑
- `src/core/services/BookmarkExporter.cpp` - 导出逻辑

### 4.4 第四阶段：UI 层（3-4天）

**学习目标：** 理解 Qt Widgets 高级用法

**学习内容：**
1. **主窗口布局**
   - 工具栏、菜单栏、状态栏
   - 分栏布局（QSplitter）
   - 视图切换（QStackedWidget）

2. **自定义视图**
   - 列表视图（QTableView + 自定义代理）
   - 卡片视图（QListView + 自定义绘制）
   - 拖拽排序

3. **对话框设计**
   - 表单布局
   - 数据验证
   - 交互流程

4. **组件设计**
   - 侧边栏（文件夹树 + 标签列表）
   - 搜索框（防抖）
   - 附加字段编辑器

**推荐阅读：**
- `src/ui/MainWindow.cpp` - 主窗口
- `src/ui/views/LinkListView.cpp` - 列表视图
- `src/ui/views/LinkCardView.cpp` - 卡片视图
- `src/ui/widgets/Sidebar.cpp` - 侧边栏
- `src/ui/dialogs/LinkEditDialog.cpp` - 编辑对话框

### 4.5 第五阶段：应用层与工具类（1-2天）

**学习目标：** 理解应用生命周期和跨平台处理

**学习内容：**
1. **应用初始化**
   - 数据库初始化
   - 主题加载
   - 系统托盘

2. **跨平台工具**
   - 浏览器打开
   - 文件路径处理
   - 系统通知

**推荐阅读：**
- `src/app/Application.cpp` - 应用初始化
- `src/utils/PlatformUtils.cpp` - 跨平台工具

---

## 5. 关键设计模式

### 5.1 Repository 模式

**目的：** 将数据访问逻辑与业务逻辑分离

**实现：**
```cpp
// 接口定义
class ILinkRepository {
    virtual QVector<Link> getAll() = 0;
    virtual int insert(const Link &link) = 0;
    // ...
};

// SQLite 实现
class SqliteLinkRepository : public ILinkRepository {
    QVector<Link> getAll() override {
        // SQLite 查询实现
    }
    int insert(const Link &link) override {
        // SQLite 插入实现
    }
};

// 依赖注入
MainWindow(ILinkRepository *linkRepo, IFolderRepository *folderRepo, ...);
```

**优点：**
- 可测试性：可以 mock 仓库进行单元测试
- 可替换性：可以轻松切换数据库实现
- 松耦合：UI 层不依赖具体数据库实现

### 5.2 异步回调模式

**目的：** 处理耗时操作（如网络请求）

**实现：**
```cpp
void FaviconService::fetchFavicon(const QString &pageUrl,
                                   std::function<void(const QString &)> callback) {
    // 1. 检查缓存
    QString cached = getCachedFavicon(pageUrl);
    if (!cached.isEmpty()) {
        if (callback) callback(cached);  // 直接回调
        return;
    }
    
    // 2. 异步网络请求
    QNetworkReply *reply = m_network->get(request);
    connect(reply, &QNetworkReply::finished, this, [this, reply, callback]() {
        // 处理响应
        if (callback) callback(localPath);  // 异步回调
    });
}
```

**使用：**
```cpp
m_faviconService->fetchFavicon(link.url, [this](const QString &path) {
    // 处理结果
});
```

### 5.3 自定义绘制代理

**目的：** 自定义列表/卡片的外观

**实现：**
```cpp
class LinkCardDelegate : public QStyledItemDelegate {
    void paint(QPainter *painter, const QStyleOptionViewItem &option,
               const QModelIndex &index) const override {
        // 1. 获取数据
        QString title = index.sibling(index.row(), 0).data(Qt::DisplayRole).toString();
        
        // 2. 绘制背景
        painter->drawRoundedRect(r.adjusted(2, 2, -2, -2), 10, 10);
        
        // 3. 绘制 favicon
        painter->drawPixmap(iconDrawRect, favicon);
        
        // 4. 绘制标题
        painter->drawText(titleRect, Qt::AlignHCenter | Qt::AlignTop, displayTitle);
        
        // 5. 绘制域名
        painter->drawText(domainRect, Qt::AlignCenter, domain);
        
        // 6. 绘制标签
        painter->drawText(tagsRect, Qt::AlignCenter, displayTags);
    }
};
```

### 5.4 信号槽连接

**目的：** 组件间通信

**实现：**
```cpp
// 信号定义
signals:
    void linkDoubleClicked(int linkId);
    void folderSelected(int folderId);
    void tagSelected(int tagId);

// 信号连接
connect(m_listView, &LinkListView::linkDoubleClicked, 
        this, &MainWindow::onDoubleClicked);
connect(m_sidebar, &Sidebar::folderSelected, 
        this, [this](int fid) {
            m_filterFolderId = fid;
            applyFilters();
        });

// 发射信号
emit linkDoubleClicked(linkId);
```

---

## 6. 常见陷阱与注意事项

### 6.1 QString 空值问题

**问题：** `QString()` 默认构造为 null string（isNull() == true），与 `QString("")` 不同

**影响：** 当 null QString 传入 `QSqlQuery::addBindValue()` 时，会被绑定为 SQL NULL

**解决：**
```cpp
// 错误
query.addBindValue(link.title);

// 正确
query.addBindValue(link.title.isNull() ? QString("") : link.title);
```

### 6.2 schema.sql 注释问题

**问题：** `executeSchema()` 按 `;` 分割后，以 `--` 开头的行会被跳过

**影响：** 如果 CREATE TABLE 前有注释行，整条语句可能被跳过

**解决：** 注释行会被自动移除，但确保 CREATE TABLE 本身不是以 `--` 开头

### 6.3 拖拽排序冲突

**问题：** `QTableView` + `InternalMove` + `setSortingEnabled(true)` 冲突

**影响：** 行移动后内部排序立即介入，导致混乱

**解决：**
```cpp
// 禁用内置排序
setSortingEnabled(false);

// 覆写 dropEvent 自定义拖拽逻辑
void LinkListView::dropEvent(QDropEvent *event) {
    // 手动处理行移动
}
```

### 6.4 跨平台加密

**问题：** 不同平台加密方式不同

**解决方案：** 使用工厂模式创建平台特定实现

```cpp
std::unique_ptr<CryptoService> createPlatformCryptoService() {
#ifdef Q_OS_WIN
    return std::make_unique<DpapiCryptoService>();
#else
    return std::make_unique<FallbackCryptoService>();
#endif
}
```

---

## 附录：文件依赖关系

```
main.cpp
  └── Application.cpp
        ├── DatabaseManager.cpp
        ├── SqliteLinkRepository.cpp
        ├── SqliteFolderRepository.cpp
        ├── SqliteTagRepository.cpp
        └── MainWindow.cpp
              ├── Sidebar.cpp
              ├── SearchBar.cpp
              ├── LinkListView.cpp
              ├── LinkCardView.cpp
              ├── LinkEditDialog.cpp
              │     ├── TagSelector.cpp
              │     └── FieldEditor.cpp
              ├── SettingsDialog.cpp
              ├── FaviconService.cpp
              ├── BookmarkImporter.cpp
              └── BookmarkExporter.cpp
```

---

## 完成状态

✅ **所有注释工作已完成！**

已为 13 个注释不足的文件添加了详细的中文注释，包括：
- 数据库接口和实现文件
- 核心服务文件
- UI 组件文件
- 应用层文件

所有源码文件现在都有完整的中文注释，便于学习和维护。

---

> **文档更新时间：** 2026-06-18
