// SyncConfigDialog.h — 同步配置对话框（Phase 3 实现）

#pragma once
#include <QDialog>

class SyncConfigDialog : public QDialog
{
    Q_OBJECT
public:
    explicit SyncConfigDialog(QWidget *parent = nullptr);
};

